#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i,n,m=0;
    printf("Nhap 1 so gi di: ");
    scanf("%d",&n);
    for(i=1; i<n; i++)
    {
        if(n%i==0)
        {
            m=m+i;
        }
    }
    if(m==n)
    {
        printf("%d la so hoan hao.",n);
    }
    else
    {
        printf("So xau qua!");
    }
    return 0;
}
